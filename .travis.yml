language: ruby
sudo: false

install:
  - true

script:
  - true

deploy:
  provider: atlas
  token: ATLAS_TOKEN
  app: citest
  edge:
    branch: sol-atlas-debug
  on:
    all_branches: true

notifications:
  email: false
# re-enable all the atlas stuff
